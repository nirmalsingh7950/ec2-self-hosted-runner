name: Python application workflow

on: [push]

jobs:
  build-linux:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: Install pip
      run: |
        sudo apt-get update
        sudo apt-get -y install python3-pip
        pip3 --version
        python3 --version
